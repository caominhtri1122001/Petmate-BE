spring:
  # Config connect DB
  datasource:
    url: 'jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}'
    username: '${POSTGRES_USER}'
    password: '${POSTGRES_PASSWORD}'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: none
    show-sql: true
  # Config flyway
  flyway:
    locations: classpath:/db/migration
    fail-on-missing-locations: false
    enabled: true
    default-schema: public
    schemas: public
  # Config spring security
  mail:
    host: smtp.gmail.com
    username: caominhtri112201@gmail.com
    password: naldykvxmitrtnpi
    port: 587


# Config swagger
springdoc:
  api-docs:
    enabled: true
    path: "/manage/api-docs"
  swagger-ui:
    enabled: true
    path: "/manage/api-docs/swagger-ui.html"
  show-actuator: false

# Config application
server:
  port: 8080

# Config log root
#logging:
#  level:
#    root: debug

# JWT
jwt:
  secret-key: '${SECRET_KEY}'

security:
  unprotect: >
    /internal/**,
    /manage/**